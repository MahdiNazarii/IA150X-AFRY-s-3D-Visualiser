using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitializeVehicleIDs : MonoBehaviour
{
    public int vehicleCount = 2;
    // Start is called before the first frame update
    void Start()
    {
        //code where we set it when we have real vehicle initialization
        
    }

    public int getVehicleCount()
    {
        return vehicleCount;
    }
}
